<?php
/**
 * 项目功能统一入口
 * 本文件集中 require 所有核心模块，index.php / refresh.php 等只需
 * `require_once __DIR__ . '/function.php'` 即可获得完整功能。
 */

// ========================================
// 1. 平台配置（数据源列表、缓存 TTL、API 路径）
// ========================================
require_once __DIR__ . '/config.php';

// ========================================
// 2. 工具函数（时间格式化、HTML 转义）
// ========================================
require_once __DIR__ . '/core/helper.php';

// ========================================
// 3. 数据抓取（API 处理器调用 + 文件缓存读写）
// ========================================
require_once __DIR__ . '/core/fetcher.php';

// ========================================
// 4. 兜底：概率触发全局过期缓存清理
// ========================================
// 说明：
//   - 有 1% 的概率扫描 cache 目录，删除超过 24 小时的旧文件
//   - 大部分时候什么都不做，不影响页面速度
//   - 作用：防止某些不再访问的平台留下永久残留文件
//   - 想调整的话：改第一个参数（概率百分比），第二个参数（过期秒数）
//   - 不需要的话，直接注释掉下面这一行即可（不影响方案A的写入时清理）
// ========================================
maybeCleanExpiredCache(1, 86400);
?>
